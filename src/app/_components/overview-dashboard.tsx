/**
 * Re-export of the Infomedia funnel dashboard UI so that app routes can
 * import it from a single canonical location within `src/app`.
 */
export { OverviewDashboard } from "@/components/funnel/overview-dashboard";
